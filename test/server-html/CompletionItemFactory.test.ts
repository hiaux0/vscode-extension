import { assert } from 'chai';
import CompletionItemFactory from './../../src/server-html/CompletionItemFactory';

describe('The Completion Item Factory', () => {

  it(`must ..`, () => {

    // arrange
    //let sut = CompletionItemFactory;

    // act
    //let result = sut.create(' ', '<input  ', 7);

    // assert
    //assert.equal(result, 'input');

  });

 

});
